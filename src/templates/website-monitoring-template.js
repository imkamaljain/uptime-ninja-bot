import dayjs from "dayjs";

export const generateWebsiteMonitoringTemplate = (name, url, status) => `
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Service Down Alert</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      color: #333;
    }
    .container {
      max-width: 600px;
      margin: 40px auto;
      background-color: #fff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 0 10px rgba(0,0,0,0.05);
      padding: 30px;
    }
    h1 {
      color: #d9534f;
    }
    .btn {
      background-color: #28a745;
      color: #fff !important;
      text-decoration: none;
      padding: 12px 20px;
      border-radius: 5px;
      display: inline-block;
      margin-top: 20px;
    }
    .footer {
      text-align: center;
      font-size: 12px;
      color: #aaa;
      margin-top: 30px;
    }
    code {
      background: #f1f1f1;
      padding: 2px 6px;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>${name} is <span style="color: red;">${status}</span></h1>
    <p>Hello,</p>
    <p>We have detected that your monitor named <strong>${name}</strong> is currently not reachable.</p>

    <ul><h3>Details:</h3>
    <li><strong>URL Checked:</strong> <code>${url}</code></li>
    <li><strong>Incident Started At:</strong> ${dayjs().format("MMMM D, YYYY, HH:MM:ss")}</li>
    <li><strong>Issue:</strong> Connection Timeout</li>
    </ul>
    
    <div class="footer">
      This alert was generated by Uptime Ninja Bot.
    </div>
  </div>
</body>
</html>
`;
